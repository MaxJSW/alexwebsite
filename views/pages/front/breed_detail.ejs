<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <title><%= breed.name %> - Caractéristiques de la Race</title>
        <meta name="description" content="Tout savoir sur <%= breed.name %> : caractère, éducation, santé. Découvrez nos reproducteurs <%= breed.name %> LOF et chiots disponibles. Élevage familial près de Montpellier.">
        <meta name="keywords" content="<%= breed.name.toLowerCase() %>, <%= breed.name.toLowerCase() %> élevage, <%= breed.name.toLowerCase() %> caractère, <%= breed.name.toLowerCase() %> registre, <%= breed.name.toLowerCase() %> reproducteurs, <%= breed.name.toLowerCase() %> chiots, élevage <%= breed.name.toLowerCase() %> france, élevage <%= breed.name.toLowerCase() %> Occitanie">
        <meta name="author" content="Élevage de Bel Air">
        <meta name="robots" content="index, follow, max-snippet:-1, max-image-preview:large, max-video-preview:-1">
        <meta name="googlebot" content="index, follow">
        <link rel="canonical" href="https://elevagedebelair.com/races/<%= breed.slug %>">
        <meta name="language" content="fr">
        <meta name="geo.region" content="FR">
        <meta name="geo.placename" content="France">
        <meta property="og:type" content="website">
        <meta property="og:site_name" content="Élevage de Bel Air">
        <meta property="og:title" content="<%= breed.name %> - Élevage de Bel Air">
        <meta property="og:description" content="Découvrez nos <%= breed.name %> LOF : reproducteurs d'exception, chiots disponibles, élevage familial et sélection rigoureuse.">
        <meta property="og:url" content="https://elevagedebelair.com/races/<%= breed.slug %>">
        <meta property="og:image" content="https://elevagedebelair.com<%= breed.firstImage ? breed.firstImage.path : '/img/og/og-image.webp' %>">
        <meta property="og:image:width" content="1200">
        <meta property="og:image:height" content="630">
        <meta property="og:image:alt" content="<%= breed.name %> - Élevage de Bel Air">
        <meta property="og:locale" content="fr_FR">
        <meta name="twitter:card" content="summary_large_image">
        <meta name="twitter:title" content="<%= breed.name %> - Élevage de Bel Air">
        <meta name="twitter:description" content="Découvrez nos <%= breed.name %> LOF : reproducteurs d'exception et chiots disponibles.">
        <meta name="twitter:image" content="https://elevagedebelair.com<%= breed.firstImage ? breed.firstImage.path : '/img/og/og-image.webp' %>">
        <meta name="twitter:image:alt" content="<%= breed.name %> - Élevage de Bel Air">
        <%- include('../front/partials/head-meta-base.ejs') %>
        <%- include('../front/partials/head-fonts-libs.ejs') %>
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@fancyapps/ui@5.0/dist/fancybox/fancybox.css">
        <link rel="stylesheet" href="/front/css/navbar.css">
        <link rel="stylesheet" href="/front/css/races.css">
    </head>
    <body class="h-100">
        <%- include('../front/frontEndAssets/header') %> 
        <main style="padding-top: 70px;">
            <section class="page-hero-simple">
                <div class="page-hero-overlay"></div>
                <div class="container">
                    <div class="page-hero-content">
                        <h1 class="page-hero-title"><%= breed.name %></h1>
                        <p class="page-hero-subtitle">
                            En savoir plus sur le <%= breed.name %>, à travers ses origines, son caractère et son standard
                        </p>
                    </div>
                </div>
            </section>
            <section class="breed-nav-section">
                <div class="container">
                    <nav class="breed-nav-buttons" data-aos="fade-up">
                        <% if (breedParagraphs && breedParagraphs.length > 0) { %>
                        <a href="#description" class="breed-nav-btn">
                        <i class="fas fa-book me-2"></i>
                        Description
                        </a>
                        <% } %>
                        <% if (breedImages && breedImages.length > 0) { %>
                        <a href="#photos" class="breed-nav-btn">
                        <i class="fas fa-images me-2"></i>
                        Photos
                        </a>
                        <% } %>
                        <% if (breedAnimals && breedAnimals.length > 0) { %>
                        <a href="#reproducteurs" class="breed-nav-btn">
                        <i class="fas fa-dog me-2"></i>
                        Nos Reproducteurs
                        </a>
                        <% } %>
                        <% if (breedPuppies && breedPuppies.length > 0) { %>
                        <a href="#chiots" class="breed-nav-btn">
                        <i class="fas fa-paw me-2"></i>
                        Chiots Disponibles
                        </a>
                        <% } %>
                    </nav>
                </div>
            </section>
            <% if (breed.description) { %>
            <section class="breed-intro-section">
                <div class="container">
                    <div class="breed-intro-box" data-aos="fade-up">
                        <p class="breed-intro-text">
                            <%= breed.description %>
                        </p>
                    </div>
                </div>
            </section>
            <% } %>
            <% if (breedParagraphs && breedParagraphs.length > 0) { %>
            <section id="description" class="breed-content-section">
                <div class="container">
                    <% breedParagraphs.forEach((paragraph, index) => { %>
                    <article class="breed-content-block" data-aos="fade-up" data-aos-delay="<%= index * 50 %>">
                        <% if (paragraph.title) { %>
                        <h2 class="breed-content-title"><%= paragraph.title %></h2>
                        <% } %>
                        <div class="breed-content-text">
                            <%- paragraph.content %>
                        </div>
                    </article>
                    <% }); %>
                </div>
            </section>
            <% } %>
            <% if (breedImages && breedImages.length > 0) { %>
            <section id="photos" class="breed-gallery-section">
                <div class="container">
                    <div class="breed-section-header" data-aos="fade-up">
                        <h2 class="breed-section-title">Galerie Photos</h2>
                        <p class="breed-section-subtitle">Découvrez la beauté de nos <%= breed.name %></p>
                    </div>
                    <div class="row g-3">
                        <% breedImages.forEach((image, index) => { %>
                        <div class="col-12 col-md-6 col-lg-4" data-aos="zoom-in" data-aos-delay="<%= index * 50 %>">
                            <div class="breed-gallery-item">
                                <a href="<%= image.path %>" 
                                    data-fancybox="gallery-<%= breed.slug %>" 
                                    data-caption="<%= image.alt || breed.name %>">
                                    <img src="<%= image.path %>" 
                                        alt="<%= image.alt || breed.name %>" 
                                        class="img-fluid"
                                        onerror="this.onerror=null; this.src='/img/placeholder-dog.jpg';">
                                    <div class="breed-gallery-hover">
                                        <i class="fas fa-search-plus"></i>
                                    </div>
                                </a>
                            </div>
                        </div>
                        <% }); %>
                    </div>
                </div>
            </section>
            <% } %>
            <% if (breedAnimals && breedAnimals.length > 0) { %>
            <section id="reproducteurs" class="breed-animals-section">
                <div class="container">
                    <div class="breed-section-header" data-aos="fade-up">
                        <h2 class="breed-section-title">Nos Reproducteurs <%= breed.name %></h2>
                        <p class="breed-section-subtitle">Découvrez nos chiens d'exception</p>
                    </div>
                    <div class="row g-4">
                        <% breedAnimals.forEach((animal, index) => { %>
                        <div class="col-12 col-md-6 col-lg-3" data-aos="fade-up" data-aos-delay="<%= index * 50 %>">
                            <div class="breed-animal-card">
                                <div class="breed-animal-image">
                                    <a href="/chiens/<%= breed.slug %>/<%= animal.slug %>">
                                    <img src="<%= animal.image.path %>" 
                                        alt="<%= animal.image.alt || animal.name %>" 
                                        class="img-fluid"
                                        onerror="this.onerror=null; this.src='/img/placeholder-dog.jpg';">
                                    </a>
                                    <div class="breed-animal-gender <%= animal.gender === 'Mâle' ? 'male' : 'female' %>">
                                        <i class="fas fa-<%= animal.gender === 'Mâle' ? 'mars' : 'venus' %>"></i>
                                    </div>
                                </div>
                                <div class="breed-animal-body">
                                    <h4 class="breed-animal-name">
                                        <a href="/chiens/<%= breed.slug %>/<%= animal.slug %>"><%= animal.name %></a>
                                    </h4>
                                    <% if (animal.breed_type) { %>
                                    <p class="breed-animal-type">
                                        <i class="fas fa-tag me-1"></i><%= animal.breed_type %>
                                    </p>
                                    <% } %>
                                    <a href="/chiens/<%= breed.slug %>/<%= animal.slug %>" class="btn-breed-animal">
                                    Voir la fiche
                                    <i class="fas fa-arrow-right ms-1"></i>
                                    </a>
                                </div>
                            </div>
                        </div>
                        <% }); %>
                    </div>
                </div>
            </section>
            <% } %>
            <% if (breedPuppies && breedPuppies.length > 0) { %>
            <section id="chiots" class="breed-puppies-section">
                <div class="container">
                    <div class="breed-section-header" data-aos="fade-up">
                        <h2 class="breed-section-title">Chiots <%= breed.name %> Disponibles</h2>
                        <p class="breed-section-subtitle">Découvrez nos adorables chiots</p>
                    </div>
                    <div class="row g-4">
                        <% breedPuppies.forEach((puppy, index) => { %>
                        <div class="col-12 col-md-6 col-lg-3" data-aos="fade-up" data-aos-delay="<%= index * 50 %>">
                            <div class="breed-puppy-card">
                                <div class="breed-puppy-image">
                                    <a href="/chiots/a-vendre/<%= puppy.breed_slug %>/<%= puppy.slug %>">
                                    <img src="<%= puppy.image.path %>" 
                                        alt="<%= puppy.image.alt || puppy.name %>" 
                                        class="img-fluid"
                                        onerror="this.onerror=null; this.src='/img/placeholder-puppy.jpg';">
                                    </a>
                                    <div class="breed-puppy-gender <%= puppy.gender === 'Mâle' ? 'male' : 'female' %>">
                                        <i class="fas fa-<%= puppy.gender === 'Mâle' ? 'mars' : 'venus' %>"></i>
                                    </div>
                                </div>
                                <div class="breed-puppy-body">
                                    <h4 class="breed-puppy-name">
                                        <a href="/chiots/a-vendre/<%= puppy.breed_slug %>/<%= puppy.slug %>"><%= puppy.name %></a>
                                    </h4>
                                    <% if (puppy.breed_type) { %>
                                    <p class="breed-puppy-type">
                                        <i class="fas fa-tag me-1"></i><%= puppy.breed_type %>
                                    </p>
                                    <% } %>
                                    <a href="/chiots/a-vendre/<%= puppy.breed_slug %>/<%= puppy.slug %>" class="btn-breed-puppy">
                                    Voir le chiot
                                    <i class="fas fa-arrow-right ms-1"></i>
                                    </a>
                                </div>
                            </div>
                        </div>
                        <% }); %>
                    </div>
                    <div class="text-center mt-4" data-aos="fade-up">
                        <a href="/chiots/a-vendre/<%= breed.slug %>" class="btn-see-all-breed-chiots">
                        <span>Voir tous nos chiots <%= breed.name %></span>
                        <i class="fas fa-arrow-right ms-2"></i>
                        </a>
                    </div>
                </div>
            </section>
            <% } %>
        </main>
        <%- include('../front/frontEndAssets/footer') %>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
        <script src="https://cdn.jsdelivr.net/npm/aos@2.3.4/dist/aos.js"></script>
        <script src="https://cdn.jsdelivr.net/npm/@fancyapps/ui@5.0/dist/fancybox/fancybox.umd.js"></script> 
        <script src="/front/js/header.js"></script><script src="/front/js/aos.js"></script><script src="/front/js/fancybox.js"></script>
        <%- include('./partials/googleAnalytics') %>
    </body>
</html>