
        <div class="col-12">
          <div class="card widget-card border-light shadow-sm">
            <div class="card-header text-bg-primary d-flex align-items-center justify-content-between">
              <span>Mots Clefs</span>
              <button id="add-keyword-btn" class="btn">
                <svg class="bi"><use xlink:href="#plus-circle"/></svg>
              </button>
            </div>
            <div class="card-body" id="keywords-container">
              <input type="text" class="form-control" id="keywords-input" placeholder="Ajouter un mot-clé...">
              <input type="hidden" id="animal-id" value="<%= animal.id %>">
              <% if (animal.keywords && animal.keywords.length > 0) { %>
                <% animal.keywords.forEach(keyword => { %>
                  <span class="badge text-bg-primary mt-2"><%= keyword %></span>
                <% }); %>
              <% } %>
            </div>
          </div>
        </div>
      </div>
    </div>